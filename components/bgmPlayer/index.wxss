/* Components/bgmControl.wxss */
.bgm{
  width: 60rpx;
  height: 60rpx;
  z-index: 999;
}

.bgm image{
  width: 100%;
}

.rotate {
  animation: music-rotate 4s linear infinite;
}

@keyframes music-rotate {
  0% {
  transform: rotateZ(0deg);
  }
  
  100% {
  transform: rotateZ(360deg);
  }
}